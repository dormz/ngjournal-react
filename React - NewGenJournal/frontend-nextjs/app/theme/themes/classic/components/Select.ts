import type { ThemeConfig } from 'antd';

/** Classic theme overrides for Select component */
export const Select: NonNullable<ThemeConfig['components']>['Select'] = {
  borderRadius: 2,
  optionSelectedBg: '#e6f7ff',
};
