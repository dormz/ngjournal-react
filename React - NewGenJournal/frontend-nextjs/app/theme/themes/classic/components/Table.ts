import type { ThemeConfig } from 'antd';

/** Classic theme overrides for Table component */
export const Table: NonNullable<ThemeConfig['components']>['Table'] = {
  headerBg: '#fafafa',
  headerColor: 'rgba(0, 0, 0, 0.88)',
};
